# Deployment

Recommended first deployment: Render as a Python web service.

- Build command: `pip install -r requirements.txt`
- Start command: `python demo_server.py`
- The server automatically uses the `PORT` environment variable supplied by the host.

Keep this demo public only with the included fictional sample data.
